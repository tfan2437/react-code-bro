import React from "react";

const Navbar = () => {
  return (
    <nav>
      <div>
        <h1>React Toolbox</h1>
      </div>
      <div>
        <a href="#">Home</a>
        <a href="#">.map()</a>
        <a href="#">onClick</a>
        <a href="#">Home</a>
        <a href="#">Home</a>
        <a href="#">Home</a>
      </div>
    </nav>
  );
};

export default Navbar;
